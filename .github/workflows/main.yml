name: Nike website

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  tests: 
    name: Python ${{ matrix.python-version }} 
    runs-on: ubuntu-latest

    strategy:
      matrix:
        python-version: [3.8, 3.9, 3.10, 3.11]

    steps: 
      - name: Checkout code
        uses: actions/checkout@v2
      
      - name: Installation de Python ${{ matrix.python-version }}
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}
      
      - name: Installation de Chrome 
        uses: browser-actions/setup-chrome@v1

      - name: Installation de dependances
        run: |
          pip install -r requirements.txt
      
      - name: Lancer les tests
        run: |
          pytest tests/ 
      # - name: Publication du HTML
      #   uses: actions/upload-artifact@v2
      #   with:
      #     name: report
      #     path: report.html

      # - name: Publication du JUnit XML
      #   uses: actions/upload-artifact@v2
      #   with:
      #     name: junit-xml
      #     path: junit.xml

  # release:
  #   name: Release
  #   runs-on: ubuntu-latest
  #   needs: tests

  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v2

  #     - name: Create Release
  #       uses: softprops/action-gh-release@v2
  #       with:
  #         tag_name: v${{ github.run_number }}
  #         name: Release ${{ github.run_number }}
  #         body: |
  #           ## Release ${{ github.run_number }}
  #           - Commit: ${{ github.sha }}
  #           - Branch: ${{ github.ref }}
  #           - Teste fait avec Python ${{ matrix.python-version }}
  #       env:
  #         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # notify:
  #   name: Notification sur Discord
  #   runs-on: ubuntu-latest
  #   needs: tests

  #   steps:
  #     - name: Envoi sur Discord
  #       uses: Ilshidur/action-discord@master
  #       with:
  #         args: |
  #           webhook_id: ${{ secrets.DISCORD_WEBHOOK_ID }}
  #           webhook_token: ${{ secrets.DISCORD_WEBHOOK_TOKEN }}
  #           content: "Tests complet  ${{ matrix.python-version }}." 
  #           Repo : ${{ github.repository }}.
  #           Version Python : ${{ matrix.python-version }}.      
